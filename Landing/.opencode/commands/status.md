---
description: Repo health status (preflight checks)
---

Run fast checks to detect common local repo blockers (Node version mismatch, broken installs).

Output:
!`npx -y @saikus08/opencode-workbench@0.1.9 doctor`
