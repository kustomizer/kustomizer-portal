---
description: Run TypeScript typecheck (if configured)
---

Run the project's typecheck script if present, otherwise try `tsc -p tsconfig.json --noEmit`.

Output:
!`npx -y @saikus08/opencode-workbench@0.1.9 run-task typecheck`
