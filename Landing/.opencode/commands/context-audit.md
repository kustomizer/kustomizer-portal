---
description: Check kit context drift (warnings only)
---

Run the drift checker and summarize the warnings.

Output:
!`npx -y @saikus08/opencode-workbench@0.1.9 audit`

If there is drift:
- Point to the exact sections to update in `openspec/project.md`.
- Do not auto-edit files unless the user asks.
